.vctl/config.yaml
.vctl/*.local.yaml
/vctl
dist/
bin/
.cache/

# Working symlinks to the production IaC repos (mirror changes there, then MR
# from those repos themselves). Never commit the symlinks into vctl.
/vault-iac
/vctl-postgres
