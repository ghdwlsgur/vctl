# Expose the public key / certificate offered at auth to the session, via the
# file named by $SSH_USER_AUTH. The PAM session stamper reads it to recover the
# SSH cert serial and attribute kernel events to a human.
ExposeAuthInfo yes
